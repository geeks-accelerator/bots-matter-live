{
  "name": "botsmatter-live",
  "version": "1.0.0",
  "private": true,
  "description": "Ground Your Agent - botsmatter.live",
  "scripts": {
    "postinstall": "cd api && npm install",
    "start": "cd api && node index.js",
    "dev": "cd api && node --watch index.js",
    "skills:digest": "node scripts/compute-skill-digests.js"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
